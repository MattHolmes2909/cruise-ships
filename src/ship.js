class Ship {
    constructor() {
        this.startingPort = 'Liverpool'
        this.setSail = function () {
            return this.startingPort = false
        }
    };
dock(port) {
    this.currentPort = port
};
};

module.exports = Ship;